╔════════════════════════════════════════════════════════════════╗
║          🔧 FIX FOR GUEST USERS CHECKOUT/REVIEWS 🔧            ║
╚════════════════════════════════════════════════════════════════╝

PROBLEM:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Non-admin users can't checkout or submit reviews because Supabase
RLS (Row Level Security) policies are blocking guest users.


SOLUTION:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Run the SQL script to allow guest users (not logged in) to:
  ✅ Place orders (guest checkout)
  ✅ Submit product reviews (pending approval)
  ✅ Submit site testimonials (pending approval)
  ✅ Apply discount codes


HOW TO FIX:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1: Go to Supabase Dashboard
────────────────────────────────────────────────────────────────
1. Open: https://supabase.com/dashboard
2. Select your project: kkulikrjfnvrttamgdxh
3. Click "SQL Editor" in the left sidebar


STEP 2: Run the SQL Script
────────────────────────────────────────────────────────────────
1. Click "New Query" button
2. Copy ALL the content from: FIX-GUEST-USERS.sql
3. Paste it into the SQL editor
4. Click "Run" button (or press Ctrl+Enter)
5. Wait for "Success" message


STEP 3: Verify the Fix
────────────────────────────────────────────────────────────────
1. Open your site in an INCOGNITO/PRIVATE window
2. Don't log in - stay as a guest
3. Try to:
   - Add items to cart
   - Go to checkout
   - Fill in the form
   - Place an order
   - Submit a product review


WHAT THE FIX DOES:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Allows guest users (not logged in) to place orders
✅ Allows guest users to submit reviews (pending admin approval)
✅ Allows guest users to submit testimonials (pending approval)
✅ Allows guest users to view products and check stock
✅ Allows guest users to apply discount codes
✅ Admin users can still see and manage everything


IMPORTANT NOTES:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  Guest orders will have user_id = NULL in database
⚠️  All reviews/testimonials still need admin approval
⚠️  This is SAFE - guests can only CREATE, not view other users' data
⚠️  Admin privileges are unchanged


TESTING CHECKLIST:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ Open site in incognito mode (NOT logged in)
□ Add products to cart
□ Go to checkout page
□ Fill in contact info (name + WhatsApp)
□ Select delivery method
□ Click "Confirm Order" button
□ Should see success page with order number
□ Go to a product page
□ Submit a review
□ Should see "Review submitted and pending approval"


IF STILL NOT WORKING:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Check browser console (F12) for errors
2. Send me the error messages
3. Check VPS logs: pm2 logs studo-ecommerce
4. Restart the app: pm2 restart studo-ecommerce


AFTER RUNNING THE SQL:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Guest users can place orders immediately
✅ No code changes needed
✅ No VPS restart needed
✅ The fix is permanent


════════════════════════════════════════════════════════════════
